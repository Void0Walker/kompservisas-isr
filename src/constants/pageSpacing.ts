/**
 * Canonical page spacing (8px grid via theme.spacing).
 * MuiContainer gutters follow PAGE_GUTTER_* — keep section-level padding aligned with these.
 */
export const PAGE_GUTTER_XS = 2; // 16px — phones
export const PAGE_GUTTER_MD = 4; // 32px — tablet
export const PAGE_GUTTER_LG = 6; // 48px — desktop

/** Space below breadcrumbs / small section separator */
export const BREADCRUMB_MARGIN_BOTTOM = 6; // 48px ≈ former 3rem
